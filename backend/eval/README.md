# Eval

This directory holds evaluation artifacts for the RAG pipeline.

- `qa_set.json` — ground-truth Q&A pairs per tenant (placeholder).
- `test_tenant_isolation.py` — asserts that tenant A cannot see tenant B's data.
  Currently skipped; will be implemented in the multi-tenancy block.
